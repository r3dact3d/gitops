---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
  name: selfsigned-api-cert
  namespace: openshift-config
spec:
  secretName: selfsigned-api-cert
  duration: 24h
  renewBefore: 23h
  isCA: false
  subject:
    organizations:
      - blinker19
  usages:
    - server auth
    - client auth
  dnsNames:
  - api.blinker19.sandbox139.opentlc.com
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
