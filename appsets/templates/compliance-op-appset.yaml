---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: compliance-op-appset
  namespace: openshift-gitops
spec:
  generators:
    - clusters:
        selector:
          matchLabels:
            argocd.argoproj.io/secret-type: cluster
            env: demo
  template:
    metadata:
      name: '{{ .Values.name }}-compliance-operator'
    spec:
      destination:
        namespace: openshift-compliance
        server: '{{ .Values.server }}'
      project: default
      source:
        helm:
          valueFiles:
            - '../../{{ .Values.name }}/values.yaml'
        repoURL: 'https://github.com/r3dact3d/gitops'
        path: common-base/openshift-compliance
        targetRevision: HEAD
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
